<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Dragon / Tools -->
    <item
        android:id="@+id/dco_menu"
        android:title="Tools"
        app:showAsAction="always"
        android:icon="@drawable/andraxicon_svg" />

    <!-- Clear logs / Trash -->
    <item
        android:id="@+id/menu_item_clear_logs"
        android:title="Clear logs"
        android:icon="@drawable/ic_trash"
        app:showAsAction="always"
        android:visible="false" />

    <!-- Tab switcher badge -->
    <item
        android:id="@+id/toggle_tab_switcher_menu_item"
        android:title="@string/toggle_tab_switcher_menu_item"
        app:actionLayout="@layout/tab_switcher_menu_item"
        app:showAsAction="always" />

    <!-- + New Tab -->
    <item
        android:id="@+id/menu_item_new_tab"
        android:title="New Tab"
        android:icon="@drawable/ic_add_box_white_24dp"
        app:showAsAction="always" />

    <!-- Overflow items -->
    <item
        android:id="@+id/menu_item_settings"
        android:title="Settings"
        app:showAsAction="never" />

    <item
        android:id="@+id/dco_menumount"
        android:title="Storage"
        app:showAsAction="never">
        <menu>
            <item android:id="@+id/menu_item_mountsdcard"  android:title="Interna" />
            <item android:id="@+id/menu_item_mount_external" android:title="Externa" />
        </menu>
    </item>

    <item android:id="@+id/menu_item_recovery" android:title="Recovery" app:showAsAction="never" />
</menu>
